# teaching-python 项目规则

- 本仓库仅用于 Python 教学工具；不得读取、写入或复用其他教学工具的业务数据。
- 所有业务 API 固定使用 `/api/python`，数据访问层固定限制 `project_type = 'python'`。
- 本地开发不得连接生产登录、数据库、运行服务或部署目录。
- 学生代码只能交给独立 Runner；业务后端不得直接执行学生代码。
- 修改现有文件时，必须保留全部现有注释、格式和未涉及内容。
